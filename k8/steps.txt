Steps:

1)create container with docker file

2) push container into docker repository account

3) Create secrets

kubectl create secret generic mongodb-secret \
  --from-literal=host=localhost \
  --from-literal=database=demo


  kubectl create secret generic postgres-secret \
    --from-literal=host=localhost \
    --from-literal=database=demo \
    --from-literal=username=mauriayllon \
    --from-literal=password=

$) with yaml files

kubectl apply -f backend-deployment.yaml
kubectl apply -f backend-service.yaml
kubectl apply -f backend-ingress.yaml

Use the ip generated by k8 cluster to use the enpoints
